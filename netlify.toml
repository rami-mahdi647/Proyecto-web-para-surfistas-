[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Stripe webhook necesita body crudo
[[headers]]
  for = "/.netlify/functions/stripe-webhook"
  [headers.values]
    cache-control = "no-store"
